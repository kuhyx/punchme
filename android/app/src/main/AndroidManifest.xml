<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Handing the target check-out time to the system Clock app via
         ACTION_SET_ALARM. Without this the intent throws a PlatformException
         ("Permission Denial ... requires com.android.alarm.permission.SET_ALARM")
         and the alarm silently never appears. A mocked channel cannot catch
         that; only a real device can. -->
    <uses-permission android:name="com.android.alarm.permission.SET_ALARM"/>

    <!-- Tap-to-clock. Reader mode (nfc_manager's checkAvailability and
         startSession) is gated on this permission, so it is needed for the
         in-app tap, not only for launch-by-tag. Declared not-required so the
         app still installs on a phone without NFC hardware. -->
    <uses-permission android:name="android.permission.NFC"/>
    <uses-feature
        android:name="android.hardware.nfc"
        android:required="false"/>

    <application
        android:label="punchme"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <!-- Tap-to-clock from outside the app. A custom MIME type routes
                 straight here with no app chooser, so a tap while the phone is
                 locked or the app is closed still lands on the coordinator.
                 With singleTop above, a tap against a running app arrives at
                 onNewIntent instead of stacking a second activity. -->
            <intent-filter>
                <action android:name="android.nfc.action.NDEF_DISCOVERED"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <data android:mimeType="application/vnd.kuhy.punchme"/>
            </intent-filter>
        </activity>
        <!-- Driven by an adb broadcast of com.kuhy.punchme.EXPORT, with the
             string extra "format". Unprotected on purpose: adb runs as the
             shell UID and could not satisfy a signature permission. What
             limits it instead is that the receiver always writes inside the
             app's own external dir, which other apps cannot read. -->
        <receiver
            android:name=".ExportReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="com.kuhy.punchme.EXPORT"/>
                <action android:name="com.kuhy.punchme.IMPORT"/>
            </intent-filter>
        </receiver>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- Package visibility for the Clock app the alarm intent resolves to. -->
        <intent>
            <action android:name="android.intent.action.SET_ALARM"/>
        </intent>
    </queries>
</manifest>
